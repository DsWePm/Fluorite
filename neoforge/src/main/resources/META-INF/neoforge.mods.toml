modLoader = "javafml"
loaderVersion = "[1,)"
license = "LGPL-3.0-or-later"

[[mods]]
modId = "caustica"
version = "${version}"
displayName = "Caustica"
description = '''Hardware-ray-traced renderer with DLSS Ray Reconstruction for the Minecraft Vulkan backend.'''
authors = "i"

# caustica-common.mixins.json is added at M3, together with the common/ source sets in build.gradle.
# Referencing it before this module compiles the shared sources would fail at load with missing targets.
[[mixins]]
config = "caustica-neoforge.mixins.json"

[[dependencies.caustica]]
modId = "neoforge"
type = "required"
versionRange = "[26.2,)"
ordering = "NONE"
side = "CLIENT"

[[dependencies.caustica]]
modId = "minecraft"
type = "required"
versionRange = "[26.2,)"
ordering = "NONE"
side = "CLIENT"
