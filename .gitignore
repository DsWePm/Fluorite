.gradle/
.idea/
# Any module's build output. Was /build/ + /buildSrc/build/ when this was a single project; the
# fabric/ and neoforge/ modules have their own, and more loaders would too.
build/
native/ngx_shim/build/
run/
run-neoforge/
out/
*.log
*.class
third_party/
bin/
/logs
